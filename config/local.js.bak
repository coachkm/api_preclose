/**
 * Local environment settings
 *
 * Use this file to specify configuration settings for use while developing
 * the app on your personal system.
 *
 * For more information, check out:
 * https://sailsjs.com/docs/concepts/configuration/the-local-js-file
 */

module.exports = {

  // Any configuration settings may be overridden below, whether it's built-in Sails
  // options or custom configuration specifically for your app (e.g. Stripe, Sendgrid, etc.)

port:8804,
BACK_WEB_URL:"https://endpoint.smartcloze.com/",
FRONT_WEB_URL:"https://app.smartcloze.com/",
PAYMENT_INFO:{


    "SECREATKEY":"", //own secret k>
    //"STRIPE_WEBHOOK_SECRET":""
 
},
  TWILLIO_ACCOUNT_SID: '',
  TWILLIO_AUTH_TOKEN: '',
  TWILLIO_NUMBER: '',
//ssl: {
//    ca: require('fs').readFileSync(__dirname + '/keys/gd_bundle-g2-g1.crt'),
  //  key: require('fs').readFileSync(__dirname + '/keys/privkey.pem'),
    //cert: require('fs').readFileSync(__dirname + '/keys/fullchain.pem')
  //},
};

